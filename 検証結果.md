# Gravity1 サンプル読取検証

AE上でGravity1_Inspect.jsxを実行し、JSON取得を確認。
原本のレイヤーやキーフレームは変更していない。

コンポ：1920×1080、60fps、1秒、ピクセル縦横比1。

| 落下物体 | 閉じたパス数 | ベジェ頂点数 |
|---|---:|---:|
| あ Outline | 3 | 48 |
| い Outline | 2 | 17 |
| う Outline | 2 | 17 |
| え Outline | 2 | 33 |
| お Outline | 3 | 46 |

合計12パス・161頂点。曲線のハンドルは別途保持。
非表示「あいうえお」は明示的に除外し、形状・変換データを取得しない。
「あかさたな」は固定指定のテキストレイヤー。文字列・変換のみ取得し、輪郭は未取得。

## 確認した構造

- 各Outlineは複数の閉じたパスを持つ。
- パスの結合が有効。塗りは非ゼロワインディング。
- ストロークのプロパティは存在するが、描画は無効。存在だけで衝突形状に追加してはいけない。
- グループのアンカーポイントが各文字で異なる。レイヤー位置だけでは輪郭を復元できない。
- SVGは今回の単純な変換構造に限定した、取得ベジェ輪郭の拡大表示。AEとのピクセル比較・衝突形状の検証は未実施。

## 次の検証

1. 一時コンポで固定テキストをシェイプ化し、足場の輪郭を取得。
2. 非ゼロ塗りと穴を維持したポリゴン化・凸分割。
3. 5物体で接触計算し、新規コンポに位置・回転を書き戻す。
4. 500物体の負荷検証。現サンプルは1秒のため、長時間試験は複製コンポで行う。

現段階は読み取り検証。物理演算・キーフレーム出力・500物体の性能は未検証。
