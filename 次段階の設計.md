# Gravity1：実証後の実装方針

## 確定した入出力

- AEから輪郭と変換情報を取得し、演算側へ渡す。
- 1レイヤーを1剛体として扱い、穴・分離したパスを保持する。
- 非表示レイヤーを除外。物理演算・固定・元アニメーション追従・除外の4分類。
- 演算結果は位置・回転をフレーム単位で新規コンポへ出力。
- アンカーポイントと物理重心の差を回転に合わせて補正する。

## 専用ウィンドウに必要な機能

1. コンポ再読込、対象物体一覧、役割の変更。
2. 輪郭と演算範囲のプレビュー。再生・停止・先頭・フレーム移動。
3. シーン重力の強さと角度、万有引力の有効化と強さ。
4. 物体ごとの密度・摩擦・反発・初速・方向・角速度・重力影響度。
5. 演算時間、精度、演算範囲の余白、退出時の除外設定。
6. 計算進捗・キャンセル、結果のキャッシュ、新規コンポへの出力。

## 性能上の方針

- ベジェ輪郭の分割と凸分割結果はキャッシュする。重力の変更で形状処理をやり直さない。
- 物理演算はAEから切り離して実行する。現在のPython実装は実証用。
- 500文字の万有引力は124,750組の計算になる。衝突のみの性能と分けて測定する。
- シーン重力・物体間引力・衝突による反発は別設定。
- 正確な物体間引力と、範囲外物体の打ち切りによる結果の変化を明示する。
- 書き戻しの進捗と中断処理を実装する。初版は全フレーム出力。

## 未検証の重要項目

- 実際の500種類の文字・漢字と複雑なフォント。
- 元のキーフレームに従って動く足場と、高速で薄い文字が接触する場合。
- 任意のパス演算子、親子関係、変形・スケールアニメーション。
- 万有引力の数値安定性、最短距離での力の制限。
- 500レイヤー分のAEへの大量書き込み時間。
- Macで配布可能なUI・演算ランタイムのパッケージ化。
